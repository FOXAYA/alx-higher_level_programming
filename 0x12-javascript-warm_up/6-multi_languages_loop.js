#!/usr/bin/node
// prints 3 lines: (like 1-multi_languages.js) but by
// using an array of string and a loop

const my_array = ['C is fun', 'Python is cool', 'JavaScript is amazing'];

for (let i = 0; i <= my_array.length; i++) {
	console.log(my_array[i]);
}

